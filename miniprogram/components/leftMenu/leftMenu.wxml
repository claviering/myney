<view class="components-left-menu">
  <view
    bindtap="selectTime"
    class="time-menu">
    <view
      wx:for="{{timeRange}}"
      data-key="{{item.key}}"
      wx:key="key"
      class="time-menu-item {{item.active ? 'active' : ''}}">
      {{item.value}}
    </view>
  </view>
</view>
  